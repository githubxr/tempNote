****

### 自评方向

- 重点：区分收敛发散边界

- 目前问题：

  - 高消耗理解
  - 容易发散
  - 容易纠结边界
  - 容易陷细节
  - 对系统整体仍陌生

- 目标

  - “把这套东西真正转化成自己的”。

    至少：

    - 微服务链路
    - token体系
    - Seata
    - MQ
    - gateway
    - relationship业务
    - 电商订单链路
    - Redis缓存
    - Nacos
    - 多环境



****

### 分布式事务Base

- 核心链路用 AT 保证强一致


- 第三方不可控，所以用 MQ 做最终一致


- 失败通过重试+补偿+人工兜底


- 避免全局事务扩大导致性能问题






****

### 项目结构笔记

##### 0508

- 脚手架自带/拓展的模块


- nacos（外置）/gateway/定时任务/mq/分库分表/鉴权


- seata/前端工程（空文件夹）等等...




- 主要的独立微服务模块如下

  - league：核心业务（会员/营销/内容/商品管理）


  - system ：账户/用户 （系统app（脚手架自带））


  - trade：交易业务模块（我们的）

    - 佣金


    - 订单




- 已有业务代码，有使用seata(only AT模式）+ShardingSphere，配置好了rocket/rabbit，but 暂无业务代码使用


- 项目中关闭了AT拦截SQL，但是依旧有AT，因为ShardingSphere集成套接了seata，所以也是有效的；


&#x09;shardingsphere.transaction.seata.at.enable=true

&#x09;替代seata.enable-auto-data-source-proxy: false





- 【用户】


&#x09;则是区分了，管理用脚手架原生的system user，达人等，则是单独建的member实体，

&#x09;且用‘多租户’方案隔离不同等级 推客/达人；

&#x09;controller分别是AuthController.java和AppAuthController.java







****

### 项目业务脉络笔记

##### 0508

在04下班前1H的需求记录基础上，细化后台部分业务

- 三个后台：小程序后台/H5后台/后台权限系统 （目前都是别人的，我们的则是分别对应项目中的xxx模块）

- 芋道预制且项目中已实现的鉴权：

  - |                | 小程序（adm） | 小程序（会员） | H5（adm） | H5（会员） |      |
    | -------------- | ------------- | -------------- | --------- | ---------- | ---- |
    | 账密图形验证码 |               |                |           |            |      |
    | 手机验证码     |               | T              |           | T          |      |
    | 微信授权       | T             |                |           |            |      |
    |                |               |                |           |            |      |
    |                |               |                |           |            |      |

    

  - 



##### 注销相关

- 会员
  - 会员表处理：is_log_off标记等...
    - 微信openid（无需处理/清理即可）
    - 会员新绑定
  - （新）注销审计记录
  - 关系处理
  - 集团解绑
  - 邀请码作废
  - 



##### 背景技术扫盲——Oauth2+opaque token

oauth2的四种授权模式+1种刷新机制

（注意，公司项目只是 **类oauth2** 的 【 Spring Security + 项目自定义 opaque token 校验】）

| 四大类型 | 项目使用 | 用途                | 补充                               |
| -------- | -------- | ------------------- | ---------------------------------- |
| 密码     | √        | 自家前端            | 手机验证码登录也属于此范畴，系变种 |
| 授权码   | √        | 三方（单点登录SSO） |                                    |
| 客户端   | √        | 应用触发            | MQ消费/ 批处理（User 变为 App）    |
| 简化前端 | ×        | 简化模式            | 过时                               |
|          |          |                     |                                    |
| 刷新机制 | ×        |                     | Token续签                          |

区分不严格：

- 如果是账户密码登录的user下单了，微服务链路就是**密码模式**
- 如果是微信授权登录后下单，那就是**授权码模式**
- 如果超时取消订单，那就是**客户端模式**（超时系统自动取消）

（实际上都是完全相同的代码在跑）



##### 项目鉴权基础链路

- 登录用到oauth2的鉴权方式：
  - 密码模式：手机验证码（H5会员/小程序Admin）
  - 微信授权（授权码）：小程序会员用到
  - 

- 微服务之间：

  - A微服务Header 透传token  -->  B微服务本地验签（本项目不使用这种方法） ； 
  - A微服务Header 登录信息json打包放header  -->  B微服务reset登录信息为A传用户；
    - 登录信息指的是：SecurityFrameworkUtils.getLoginUser()
    - reset的意思是：SecurityFrameworkUtils.setLoginUser(loginUser, request)
    - SecurityFrameworkUtils原理：通过 **ThreadLocal** 绑定鉴权信息到请求 线程 （only）；
      - 通过SpringSecurity 操作存入ThreadLocal & SecurityContextHolder上下文
    - SecurityFrameworkUtils.getLoginUser()随时都可调用，里面的user生命周期是：
      - 请求结束就销毁；
      - 每次请求进来，都会被拦截器（TokenAuthenticationFilter）塞入ThreadLocal，拦截器会走之前说的if json then 直接用 else if token then查redis/db
    - 注意：@Async之类的子线程是拿不到的；需要手动折腾传递；

  

-  前端后端之间： 

  - 前端header透传token  -->  gateway --1min缓存未中-> 调用system.checkToken--> redis --未中--> DB；
  - 前端header透传token -->  gateway本地验签（本项目不使用）

- gateway统一鉴权
  - A微服务Header 透传 .  B微服务本地验签 （本项目不用）
  - gateway调用system.checkToken





##### 项目鉴权总结

- 采用了 Gateway 统一鉴权 + Opaque Token 的微服务安全架构 + SpringSecurity
  亮点在于：
  - 通过 Gateway 本地缓存 解决了 Opaque Token 频繁查库的性能瓶颈
  - 通过 Header 透传用户上下文 实现了微服务间的无状态高效通信



##### 鉴权比对

- 传统项目中：session/cookie是，浏览器存cookie当‘token’ 实际信息存单体项目的内存中名为session


- 此微服务项目中：就是token发挥了类似于‘cookie’的作用？ 而原本是单体项目中jvm内存中的‘session对象’存的登录完整信息，被挪到了db中（还用redis缓存）







##### 短信场景下的SpringEvent应用案例

- Cause：
  - 短信发送/验证都是在system内，因此用单体的SpringEvent

- 流程：

  - H5会员登录接口 --> service --feign--> system.sendMq：1生成验证码存sms_code表，2调用 --> sendSingleSms：1校验模板/渠道，2存log，3调用 --> smsProducer.sendSmsSendMessage（applicationContext.publishEvent()发SpringEvent（此处是生产者）） --> 
    	消费者（system）：SmsSendConsumer.onMessage()(@EventListener + @Async)  -->  doSendSms调用三方SMS API
  - 最终， 手机收到短信，然后，登陆时用户填入短信验证码，传入登录接口，然后会查sms表核对是否匹配...

  - 



##### 项目鉴权结构配置梳理

- 特征：OncePerRequestFilter为主的一堆自定义Filter预切出校验/存Token什么什么的；
- 基本结构：
  - TokenAuthenticationFilter：
    - 角色：解决 **你是谁** 
    - 描述：此Filter来根据Token，去SecurityContextHolder 查 LoginUser
  - @PreAuthorize(menu菜单项 权限key)：
    - 角色：用于检查 **你是否有权限**
    - 描述：用来标记要被权限控制的接口
    - 补充：【此注解属SpringSecurity】
  - PermissionServiceImpl：
    - 角色：检查权限的具体逻辑写在这
    - 描述：k实际处理访问被@PreAuthorize标记的接口前，的权限查询



##### 项目配置的短信

- 一套**通用短信模块**，给后台系统统一管理短信发送：

  1. 对接阿里云、腾讯云、华为云、七牛云等多家短信服务商
  2. 短信**异步发送**（用 Spring Event  /  RocketMQ 消息队列），不阻塞业务
     - 目前用的是spring event，因为发送/回调短信都在system内完成

  3. 提供两套接口：
     - 通用发送短信接口 `SmsSendApi`：发通知类短信
     - 验证码专用接口 `SmsCodeApi`：登录、找回密码等验证码，自带频率限制
     - 短信回调接口`/admin-api/system/sms/callback/aliyun`：
  4. 有完整后台：配置 渠道、配置模板、看发送日志、查回调状态

  】



##### 项目链路梳理

- H5/会员/短信登录
  - 链路：接口 => (校验：1-校验验证码；2-通过手机查用户) => (oauth2TokenApi.创建Token) => 日志记录 => 返回Token

  - Detail：oauth2TokenApi.创建Token具体步骤：


- 推客小程序/会员/注销

  - 链路：

    ```
    接口
    获取当前登录用户
    审计记录
    处理上下级关系
    	下级改绑到当前用户上级
    	relationship旧关系失效
    	新建继承关系
    集团解绑
    会员表停用
    	清理手机号/name/openid
    	is_log_off标记注销
    踢出登录
    ```

  - 关键业务点：

    ```
    注销后需要踢出登录
    注销后还能看到旧佣金
    	原因：注销前和注销后新微信绑定的账号，都是微信端给的同一个openid，所以得清理
    	搁置：产品最新消息是，注销后不让继续新注册使用我们平台，所以可以不做
    ```

    



##### MQ应用案例

- 流程
  - 







****

****

##### MVC少见用法

- @RequestMpping/@PostMapping 写在Interface上，实现的controller的接口就不写了...
- 







##### 项目业务完整表述

```
‘推客’项目
	角色：属于微信推出的 ‘推客生态’中的一员（非电商，而是带货/记账 平台/佣金分销管理系统,属于多个三分平台合作的一环）
	结构：属于微信小程序, 内容为‘套娃’包裹了许多【分享链接】，客户点击链接，可跳转到微信其它电商小程序的：【带货短视频/带货直播间/商品页面分享】的三方小程序页面进行【购物】
	目标客户：微信用户
	基本业务链路：
		in微信：微信用户点击‘推客’链接进入我们的‘推客’微信小程序
		（如果首次注册，会先：绑定手机号成为‘会员’ / 系统自动绑定‘上级’为‘分享者’（用户是‘被分享者’））
		in我们的推客小程序：有身份等级在我们平台后，再从我们的推客小程序，跳转到目标电商平台小程序
		in三方电商平台：如果用户通过【分享链接】进入三方电商平台，且购买了商品，那么我们的‘推客小程序‘就会得到佣金，并且此顾客的’上级用户‘也会得到‘分成佣金’
		in我们的推客小程序：如果用户下单了，我们被同步订单状态
		in用工平台： 灵活用工平台是三方兼职平台，推客在三方平台绑定；提现才绑定
						
	客户等级机制（小程序）：普通会员 =分享给其他人=> 推客 =(配置身份)=> 达人
	特殊等级机制（H5）：小程序会员==(配置身份)==> 服务商（1星团长） ==> 招商总监（2星团长）
	
	会员角色：可自购/也可通过【短视频，直播间，商品】分享给别人来赚取佣金；用户要被分享才可下单 分享者是上级
	会员双端身份：会员在【推客小程序】和【H5】两端，各有1个独立账户，账户名称相同（类比在小学和中学都有学籍）
		小程序能做所有会员操作，h5页面只能看分多少钱/提现
	 	在小程序身份最高是是达人（余额只有他当达人的时候赚到的钱） 
	 	在h5平台是团长（团长所有钱能在h5看） 
	
	
	项目结构（鉴权是Oauth2+opaque,通过url前缀区分admin和member)：
		推客小程序后端：
			admin后台
			member会员
		H5后端：
			admin后台
			member会员
		权限认证系统：
			统一认证
	其中，
	后台权限认证系统 是能看到/配置所有权限/信息的
	admin后台均是我们的运营管理后台
	小程序则是会员/客户功能
	小程序和h5后端均有对应前端工程（前后端分离，前端工程就是微信小程序前端）
	
	其中，小程序的所有数据到h5单向（例如h5后端不能调小程序后端的微服务）
	
	
	===============
	first 开发任务存根：注销功能，不能登录，权益没， 手机之类的删掉放到备注 如果同时有小程序达人和h5团长，h5那边也要找一下，看是否同步注销（有关联） 多业务表，可能还有三方都要同步执行，账号，关系，订单，提现，记录
```



术语‘绕圈子




