****

### 自评

- 重点：区分收敛发散边界

- 目前问题：

  - 高消耗理解
  - 容易发散
  - 容易纠结边界
  - 容易陷细节
  - 对系统整体仍陌生

- 目标

  - “把这套东西真正转化成自己的”。

    至少：

    - 微服务链路
    - token体系
    - Seata
    - MQ
    - gateway
    - relationship业务
    - 电商订单链路
    - Redis缓存
    - Nacos
    - 多环境



****

### 分布式事务Base

- 核心链路用 AT 保证强一致


- 第三方不可控，所以用 MQ 做最终一致


- 失败通过重试+补偿+人工兜底


- 避免全局事务扩大导致性能问题






****

### 项目结构笔记

##### 0508

- 脚手架自带/拓展的模块


- nacos（外置）/gateway/定时任务/mq/分库分表/鉴权


- seata/前端工程（空文件夹）等等...




- 主要的独立微服务模块如下

  - league：核心业务（会员/营销/内容/商品管理）


  - system ：账户/用户 （系统app（脚手架自带））


  - trade：交易业务模块（我们的）

    - 佣金


    - 订单




- 已有业务代码，有使用seata(only AT模式）+ShardingSphere，配置好了rocket/rabbit，but 暂无业务代码使用


- 项目中关闭了AT拦截SQL，但是依旧有AT，因为ShardingSphere集成套接了seata，所以也是有效的；


&#x09;shardingsphere.transaction.seata.at.enable=true

&#x09;替代seata.enable-auto-data-source-proxy: false





- 【用户】


&#x09;则是区分了，管理用脚手架原生的system user，达人等，则是单独建的member实体，

&#x09;且用‘多租户’方案隔离不同等级 推客/达人；

&#x09;controller分别是AuthController.java和AppAuthController.java







****

### 项目业务脉络笔记

##### 0508

在04下班前1H的需求记录基础上，细化后台部分业务

- 三个后台：小程序后台/H5后台/后台权限系统 （目前都是别人的，我们的则是分别对应项目中的xxx模块）

- 芋道预制且项目中已实现的鉴权：

  - |                | 小程序（adm） | 小程序（会员） | H5（adm） | H5（会员） |      |
    | -------------- | ------------- | -------------- | --------- | ---------- | ---- |
    | 账密图形验证码 |               |                |           |            |      |
    | 手机验证码     |               | T              |           | T          |      |
    | 微信授权       | T             |                |           |            |      |
    |                |               |                |           |            |      |
    |                |               |                |           |            |      |

    

  - **阐述**：小程序会员的”账密登录“/”手机号登录“其实都是“芋道脚手架（咱们二开）自带的，接口看起来也都写好了，实际上是，咱们没用到，不用管；【小程序会员只有’微信授权登录‘，h5会员则只有手机号+手机验证码登录，其它的不用管（即便看起来都是写好了的...我当时就看到手机号登录没有校验密码。。。以及验证码也没做。。。就很困惑，询问多次才得知是，脚手架自带，我说那不屏蔽掉？他们说没关系，里面没对其，没法’卡登录‘的，我说可是这会误导开发者）；尤其搞的我不爽的是，我是先后问了2人，3次，前面的我都说“小程序后台member登录” 为什么没校验密码和手机验证码，是有什么特殊处理我没看到吗？第一次是没说清，第二次的人是有点不耐烦和笑说他们是用微信授权，我说我记得有三种登录方式都做好了，s1是手机号+手机验证码，s2是账密登录，s3是微信授权，你是说，1手机号+验证码也是调的微信授权？他当时就好像是’我笨‘一样的笑和’无奈‘，然后我反复找，资料，以及说翻出自己之前的理解，先是把之前业务在’原型‘上的‘小程序后台’，‘小程序’，‘h5后台’，‘h5’上对于‘小程序后台是后台admin管理，小程序则是前台用户管理，是两个不同的前端，h5类似，的理解我反应过来那就是其实并不是不同的前台页面，而只是不同的admin和会员这种账户类型在同一个前端/对于后台程序能看到的不同界面，以及这时他才说，s1和s2是脚手架自带，我们小程序member是只用s3微信授权。。。我说那就不管S1和S2...也就是说，他前面多次搞错把？可是s1,s2都没有被屏蔽，看起来也’正常‘但又缺逻辑，作为要有基本鉴权理解， 我肯定得看啊。。。】





##### 注销相关

- 会员
  - 会员表处理：is_log_off标记等...
    - 微信openid（无需处理/清理即可）
    - 会员新绑定
  - （新）注销审计记录
  - 关系处理
  - 集团解绑
  - 邀请码作废
  - 



##### 背景技术扫盲——oauth2+opaque token

oauth2的四种授权模式+1种刷新机制

| 四大类型 | 项目使用 | 用途                | 补充                            |
| -------- | -------- | ------------------- | ------------------------------- |
| 密码     | √        | 自家前端            |                                 |
| 授权码   | √        | 三方（单点登录SSO） |                                 |
| 客户端   | √        | 应用触发            | MQ消费/ 批处理（User 变为 App） |
| 简化前端 | ×        | 简化模式            | 过时                            |
|          |          |                     |                                 |
| 刷新机制 | ×        |                     | Token续签                       |

区分不严格：

- 如果是账户密码登录的user下单了，微服务链路就是**密码模式**
- 如果是微信授权登录后下单，那就是**授权码模式**
- 如果超时取消订单，那就是**客户端模式**（超时系统自动取消）

（实际上都是完全相同的代码在跑）



##### 项目鉴权

- 登录用到：
  - 密码模式：
  - 微信授权（授权码）：
  - 

- 微服务之间：

  - A微服务Header 透传token  -->  B微服务本地验签（本项目不使用这种方法） ； 
  - A微服务Header 登录信息json打包放header  -->  B微服务reset登录信息为A传用户；
    - 登录信息指的是：SecurityFrameworkUtils.getLoginUser()
    - reset的意思是：SecurityFrameworkUtils.setLoginUser(loginUser, request)
    - SecurityFrameworkUtils原理：通过 **ThreadLocal** 绑定鉴权信息到请求 线程 （only）；
      - 通过SpringSecurity 操作存入ThreadLocal & SecurityContextHolder上下文
    - SecurityFrameworkUtils.getLoginUser()随时都可调用，里面的user生命周期是：
      - 请求结束就销毁；
      - 每次请求进来，都会被拦截器（TokenAuthenticationFilter）塞入ThreadLocal，拦截器会走之前说的if json then 直接用 else if token then查redis/db
    - 注意：@Async之类的子线程是拿不到的；需要手动折腾传递；

  

-  前端后端之间： 

  - 前端header透传token  -->  gateway --1min缓存未中-> 调用system.checkToken--> redis --未中--> DB；
  - 前端header透传token -->  gateway本地验签（本项目不使用）

- gateway统一鉴权
  - A微服务Header 透传 .  B微服务本地验签 （本项目不用）
  - gateway调用system.checkToken





##### 项目总结

- 采用了 Gateway 统一鉴权 + Opaque Token 的微服务安全架构
  亮点在于：
  - 通过 Gateway 本地缓存 解决了 Opaque Token 频繁查库的性能瓶颈
  - 通过 Header 透传用户上下文 实现了微服务间的无状态高效通信





##### 鉴权比对

- 传统项目中：session/cookie是，浏览器存cookie当‘token’ 实际信息存单体项目的内存中名为session


- 此微服务项目中：就是token发挥了类似于‘cookie’的作用？ 而原本是单体项目中jvm内存中的‘session对象’存的登录完整信息，被挪到了db中（还用redis缓存）







##### 短信场景下的SpringEvent应用案例

- Cause：
  - 短信发送/验证都是在system内，因此用单体的SpringEvent

- 流程：

  - H5会员登录接口 --> service --feign--> system.sendMq：1生成验证码存sms_code表，2调用 --> sendSingleSms：1校验模板/渠道，2存log，3调用 --> smsProducer.sendSmsSendMessage（applicationContext.publishEvent()发SpringEvent（此处是生产者）） --> 
    	消费者（system）：SmsSendConsumer.onMessage()(← @EventListener + @Async)  -->  doSendSms调用三方SMS API
  - 最终， 手机收到短信，然后，登陆时用户填入短信验证码，传入登录接口，然后会查sms表核对是否匹配...

  - 



##### MQ应用案例

- 流程
  - 







****

****

##### MVC少见用法

- @RequestMpping/@PostMapping 写在Interface上，实现的controller的接口就不写了...
- 



