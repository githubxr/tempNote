==========================================================\### 草稿区mysql8 默认密码：zdwo\_MVPg5jk 重置为了123456；端口3308后端导入成功后，默认管理员账号：用户名: admin密码: admin123这个账号在 ruoyi-vue-pro.sql 中已经初始化好了。重装mysql得先后执行sc delete MySQLsc.exe delete MySQL&#x20;如果连不上Nacos，可以在本地配置文件中直接配置数据库：&#x20;application-local.yamlspring:&#x20; datasource:&#x09;...分库分表，mq都有集成，但是业务代码并没全用到==========================================================\###按工作日划分的极简日志（数字代表入职的第几个工作日，例如1代表05月07日）环境：问题解决/全部跑通（1）扫盲：分库分表/seata（2）项目结构简单梳理（2）seata（前半50%-本地配置）（3）准备开发任务：梳理【注销需求】涉及模块和边界+参照已有seata用法 （3）计划下个工作日（seata（后半100%-简单应用））==========================================================\##记录方案初步规划核心链路用 AT 保证强一致第三方不可控，所以用 MQ 做最终一致失败通过重试+补偿+人工兜底避免全局事务扩大导致性能问题==========================================================\#项目结构笔记0508脚手架自带/拓展的模块&#x09;nacos（外置）/gateway/定时任务/mq/分库分表/鉴权&#x09;seata/前端工程（空文件夹）等等...主要的独立微服务模块如下\-league：核心业务（会员/营销/内容/商品管理）\-system ：账户/用户 （系统app（脚手架自带））\-trade：交易业务模块（我们的）&#x09;佣金&#x09;订单已有业务代码，有使用seata(only AT模式）+ShardingSphere，配置好了rocket/rabbit，but 暂无业务代码使用项目中关闭了AT拦截SQL，但是依旧有AT，因为ShardingSphere集成套接了seata，所以也是有效的；&#x09;shardingsphere.transaction.seata.at.enable=true&#x09;替代seata.enable-auto-data-source-proxy: false==========================================================\#业务脉络笔记0508在04下班前1H的需求记录基础上，细化后台部分业务\-三个后台：小程序后台/H5后台/后台权限系统 （目前都是别人的，我们的则是分别对应项目中的xxx模块）\-==========================================================\###日志区\##0507\- 安装开发环境，程序处理 清理不知密码残留的旧mysql等程序并重装\- git 拉项目，准备跑起来\--maven 拉依赖 发现不存在maven依赖的沟通解决清理，\--本地部署项目web，数据库/redis/nacos(连远程）\-本地跑起来后端，打开swagger/简单鉴权得token登录跑通\--梳理业务\---先简单结合readme和ai梳理 熟悉项目\---结合项目uml，沟通梳理目前主要业务脉络和进度\##0508\--旧环境jdk清理重装（旧jdk17有问题没法启动seata，官网下的新jdk17重装就没问题了。。。）\--复制到本地nacos（避免seata回滚同事的服务）\-拓展\--分库分表扫盲\--redis集群扫盲\--seata/分库分表扫盲（100%）\##0509\-seata上手（50%-完成本地配置）&#x09;环境排雷/配置链路打通/梳理业务相关链路（然后才能明确方案【seata.AT / Saga / Tcc ...】）（推进中50%）&#x09;（项目存在可参考业务实现链路，AT模式的seate和ShardingSphere集成分布式事务 （50%推进中））&#x09;（可能AT+MQ是最合适的）\-本地的，写一个简单seata案例测试是否回滚一致性 搁置\-模仿写一个“同结构方法”再调试，👉 不要自己设计！todo\-准备注销功能落地 todo==========================================================\#问题记录\-0507\- JDK17和nacos2.5.2不兼容，得改为2.2.3，否则会出现成功启动，然后闪现几行后闪退\-有时redis会默认开机自启，此时如果不知道，还手动redis-server.exe redis.windows.conf，那么也会’一闪而过闪退‘，不用管，直接用即可；\-ping 和telnet，浏览器 走不同协议，网络可达性方面，不能一个代表全部\-0508\- 旧环境jdk清理重装（旧jdk17有问题没法启动seata【报错Active code page: 65001】）\--解决：官网下的新jdk17重装就没问题了\-下了最新版idea，不知道Environment Variables被藏哪去了；&#x09;application也始终在上面没法移到services里，重新安装旧版idea解决；###### \-0509seata启动报错：【Caused by: com.alibaba.nacos.api.exception.NacosException: failed to req API:/nacos/v1/ns/instance after all servers(\[localhost:8848]) tried: java.net.ConnectException: Connection refused: no further information】---原因：【seata-server\\conf\\application.yml】除了seata的config配置nacos地址，register也要配！否则会默认8848\---解决：为registry也添加\---附注：可以不填账密，因为nacos.core.auth.enabled 默认 =falseseata server启动后，Store Mode / 依旧是 file&#x09;原因和解决方案：虽然nacos 的seataServerTest.properties文件名和seata的yml配置的data-id匹配，但是nacos中，配置格式勾选的是Text！且nacos.group也不匹配&#x09;解决方案：修改配置格式为Properties，group也修正为匹配seata server启动报错：The driver {com.mysql.cj.jdbc.Driver} cannot be found in the path ... seata-server/lib/jdbc&#x09;原因：\\seata-server\\lib\\jdbc\\目录需要jdbc.jar&#x09;解决：从本地maven仓库复制一个【mysql-connector-j-9.5.0.jar】过去order起不来报错找不到Seata server&#x09;原因：测试环境参考的命名空间有两个看起来配置都是11个，但是我拷贝的seataServerTest里面缺了一个配置&#x09;解决：补上【service.vgroupMapping.default\_tx\_group=default】即可&#x09;补充：这个虚拟集群名称是必须的，默认就叫default&#x09; 昏头错误：把web微服务本地resources里的application-local.yml里spring.cloud.nacos.config.group改为了seata\_group，这个别乱写;&#x09;若还不行，就直接在web微服务的优先级yml里的seata.service.default加上grouplist: 127.0.0.1:8091 来强制直连==========================================================\#笔记0508分库分表垂直拆分了解的原因是，项目中集成且使用在业务代码，得扫盲要实现的注销功能也会设计涉及\-reason：\--垂直拆分\---分库（localhost分databse性能没什么区别，一般是分到另一个主机）：\---分表（按业务列把不常用列分出去）：比对“主表从表”的优势：若不分，就算我只select 指定的部分行，数据库在以“行”为单位 查询时，也会把那几千字节的“自我介绍”也加载 到内存\--水平拆分：\---分表：表存在不同db\---分行：表拆为多表，每表存几万条之类的，按id（范围路由/hash取模）之类的取模seata&#x09;结构上：分client/server&#x09;	client：集成seata的微服务应用&#x09;		（每个微服务数据库都得建立UNDO\_LOG的sql表脚本从github找对应版本）&#x09;	server：单独下载的seata，通过seata-server.bat启动，http://localhost:7091访问管理&#x09;		（如果使用默认的file模式而非db，就不需要配置使用seata-server\\script\\server\\db\\mysql.db建表 for seata server）&#x09;任务上，分注册和存储&#x09;	注册：推荐注册到nacos，这样其他微服务能找到（不注册的话，每个客户端都得yml指定seata server地址）&#x09;	存储：这个是默认file还是什么db，还会自动建立什么log什么什么表？（seata2.6.0为例）项目中有可参考的，seata会和ShardingSphere搭配集成使用以保证分布式一致性本地调试自动覆盖地址（实现原理后面研究，这里记录用法）&#x09;IDEA 启动配置：&#x09;Run → Edit Configurations&#x09;在 Environment Variables 中添加：&#x09;	SPRING\_CLOUD\_NACOS\_SERVER\_ADDR=localhost:8848\##0509seata使用\--不用file模式，db模式部署，\---需要&#x09;1 配置seata server 数据库：将  \[\\seata-server\\script\\server\\db\\mysql.sql] 部署到seata server主机的数据库，&#x09;2 配置注册到nacos/读取nacos配置/db记录事务：&#x09;	 \\seata-server\\conf\\application.yml配置nacos地址+ seata server 的db地址&#x09;		（推荐用nacos中建立seataServer.properties统一配置）即可&#x20;      	 	seata server的application.yml三个重要配置：config/registry/store&#x09;3 nacos的seataServer.properties是导入同事的，所以无需折腾【seata-server\\script\\config-center\\config.txt】&#x09;	如果想折腾，参照附录01&#x09;  4 启动后，记得检查 use lock store mode: db / use session store mode: db /use store mode: db &#x09;	是否是db，如还是默认的file说明没读到nacos配置&#x09;  5 配置client表：每个微服务节点的db都得加undo\_log表；\--然后，在微服务项目， 建个简单分布式事务测试（用AT模式，and 必须至少2个微服务节点），手动触发下异常回滚看看熟悉推客项目seata+ShardingSphere的用法（50%推进中）熟悉在项目中【注销需求】涉及服务和边界（todo）争取用【依葫芦画瓢】来加速【注销需求】的落地涉及的业务逻辑（todo）Nacosnacos命名空间	全方面隔离配置文件/注册服务&#x09;yml中可空着namespace，默认就是publicnacos的配置文件可覆盖且优先级比resource下的yml高；&#x09;==========================================================###### 待办seata日志后期解决：ELK（Elasticsearch + Logstash + Kibana）来收集日志seata启动：分布式锁🔒的表未初始化先不管：ERROR --- \[lock.DataBaseDistributedLocker] : The distribute lock table is not config==========================================================###### 附录01导入seata配置到nacos教程：第一步：去你 本地 Seata 的 conf 文件夹路径：plaintextD:\\workspace\\apache-seata-2.6.0-incubating-bin\\seata-server\\conf第二步：找到这个文件plaintextnacos-config.shWindows 用：nacos-config.cmd第三步：双击运行它！它会自动把 Seata 需要的所有配置 → 导入本地 Nacos