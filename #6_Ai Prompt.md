****



##### 笔记原则

- 任何笔记内容都要有 ’**主线**‘ 来串联
  - 因果链条 / 状态流 



##### 技术相关Prompt

- 初次询问技术：
  - $xx技术{例如饭型擦除} 究竟要理解何程度？ 每次都是一大篇‘课文’，类似应试教育知识点不说人话，明明两三段能说清非要搞好几页背景介绍/相关技术概述/相关技术背景/xx角度 + 要么就是火星文一样的‘蓄意编造



##### 业务理解格式

- 优先用Markdown缩进 而非动不动用XMind 表示流程：完全透彻之前不要用XMind，用markdown的【精准抽象类】缩进即可，就类似于下面内容中的【流程】：

  - ```
    ##### 短信场景下的SpringEvent应用案例
    - Cause：
      - 短信发送/验证都是在system内，因此用单体的SpringEvent
    - 流程：
      - H5会员登录接口 --> service --feign--> system.sendMq：1生成验证码存sms_code表，2调用 --> sendSingleSms：1校验模板/渠道，2存log，3调用 --> smsProducer.sendSmsSendMessage（applicationContext.publishEvent()发SpringEvent（此处是生产者）） --> 
        	消费者（system）：SmsSendConsumer.onMessage()(← @EventListener + @Async)  -->  doSendSms调用三方SMS API
      - 最终， 手机收到短信，然后，登陆时用户填入短信验证码，传入登录接口，然后会查sms表核对是否匹配...
    
      - 
    
    
    ```

    









用户正在参与一个涉及“IS业务类型与产品类型关系变更”的中后台系统功能重构项目，任务内容包括读取和理解账单中心/报障系统等多个页面的老旧代码（屎山），梳理前后端数据结构和映射逻辑，更新本地字典表并补全中英文国际化，涉及复杂的 queryMsgList、queryGraph 等重复调用的巨大方法和数据流。用户已持续输出日报和笔记，有完整的流程分析和改造记录。.





CodeX公益站

xiaoqiang

xiaoqiang26330





new GPT

jeumanvenolas@mail.com----邮箱密码：NXJTIqFAAuEJ----GPT密码：4f!gqhu17Amo--------TG电报群：https://t.me/+Rn8hHq0gppc4YzJh----chatgpt使用说明：https://docs.google.com/document/d/197wyh-V-BzbTn7LEJLbLqd9EB3mHIXVmjdAX0-uYwqw

new PWD：Mail@990803_















##### sec1

```
不一定说明“你技术被评判不咋地”。更像是：

你被丢到了一个**没人完全说得清、但又需要有人推进的遗留业务坑**里。

这类活通常有几种含义，不能自动往“我不行所以被安排脏活”上解读。

**更可能的解释**

1. **项目本身优先级尴尬**
   不是新业务、不是核心迭代、不是老板每天盯的漂亮项目，但线上/测试/产品一直说有问题，所以需要有人处理。

2. **老代码没人想碰**
   平账、结算、报税这种账务逻辑，改错了很容易背锅。老员工如果也不熟，可能就自然漂到新人这边。

3. **你是后端，且刚好能接**
   入职 1 个月，公司不会马上把核心新架构交给你，但会把“能验证你排查能力、沟通能力、代码理解能力”的事情给你。

4. **这也可能是在观察你**
   不是观察你能不能炫技，而是看你能不能把混乱问题拆清楚：复现、定位、确认口径、控制风险、交付修复。

所以我的判断：**这不是明显的“看不起你技术”，但确实是一个低规范、高甩锅风险的任务。**

你真正要警惕的不是任务本身，而是这句话：

> “平账结算相关还有啥的有 bug。”

这句话太虚了。虚到不适合作为开发任务。

**结合文档看，核心冲突很明显**

早期会议纪要里有两套味道不一致的规则。

第一段说：

> A 800、B 600、C 500，出账 850，先扣 A 800，再从 B 扣 50。

这是**可跨账户组合扣款**。

但注意点 2 又说：

> 平账结算时，是全部数据统一找一个账户平账，账户余额不足时，不会处理。

这是**只能找单一账户扣款，不能组合**。

产品现在说“文档里的平账规则大概率是只能扣一个，不能组合”，那就说明：  
**这个项目最大的 bug 源头，很可能不是代码，而是业务口径前后不一致。**

而你现在看到的代码，确实也像经历过多版平账算法：旧算法、最大程度算法、按账户权重、按运单分组、批量优化，全混在一起。也就是说，代码混乱不是你造成的，是需求演化和维护不规范造成的。

**你现在应该怎么定位自己的任务**

你跟产品说的方向基本对：

> 本地跑起来，找测试复现问题，修复，再让 AI 辅助看代码/写测试，边界功能手工测。

这个方向没问题，但还要加一句关键保护：

**先确认“当前正确规则是什么”，再修。**

你可以把任务正式化成这几步：

1. 跑通本地环境和基础导入流程。
2. 找测试拿一条可复现 bug：输入数据、操作步骤、期望结果、实际结果。
3. 找产品/测试确认平账规则，尤其是：
   - 能不能跨账户组合扣？
   - 是按单条出账平，还是按同一运单合计平？
   - 账户选择顺序按什么？余额、权重、创建时间、账户名称？
   - 余额不足时是跳过、部分扣、还是记录待结算？
   - 月结后修改/删除入账出账，历史结算怎么处理？
4. 修一个问题只覆盖一个明确口径。
5. 补最小测试或对账 SQL，至少能证明这次案例不会再错。
6. 让测试按同一份数据验收。

**你可以这样跟产品/测试说**

比较稳的说法是：

> 我先不直接改平账算法，因为文档里存在“跨账户扣款”和“只能单账户扣款”两种描述。为了避免修成另一个规则，我先找测试拿一组当前线上/测试环境可复现的数据，然后和你们确认这组数据的期望结果。确认后我按这个口径修，并补一份最小回归用例或对账 SQL，后续类似问题也能快速判断。

这句话很重要。它不是推活，是把锅从“你猜错了”变成“大家确认规则后我实现”。

**对你个人价值**

如果你能把这个项目梳理出来，其实挺加分。因为这不是简单写接口，而是典型后端硬活：

- 读遗留代码
- 理解账务规则
- SQL 排查
- Excel 导入导出
- 事务和并发
- 数据一致性
- 和产品/测试对齐验收口径

但你要避免陷入“我一个人把历史烂账全修好”的幻觉。你现在最优策略不是大重构，而是：

**抓一个明确 bug，复现它，修掉它，沉淀一条测试/对账用例。**

连续做几次，你就从“被安排折腾旧项目的人”，变成“这个账务系统现在只有你讲得清的人”。这在职场上反而是筹码。
```



##### sec2

```
我觉得**“周五前把问题解决”是有条件合理**的，不是天然不合理。

如果目标是下面这种，就合理：
1. 只修 **1 到 2 个明确、可复现的问题**
2. 口径已经能确认，或者你今天就能确认
3. 能拿到测试数据，最好是测试同学已经踩出来的案例
4. 先恢复正确性，再补最小验证

如果目标是下面这种，就不太合理：
1. “把平账/结算相关所有 bug 都收掉”
2. 还没确认业务口径，尤其是“能不能跨账户组合扣”
3. 还要顺手把老代码重构干净
4. 还要求顺便补全所有测试

你这个项目里，文档和代码都显示它不是单点小 bug，而是**规则和实现都可能有历史分歧**。所以更现实的说法是：

**周五前完成“一个明确问题闭环”是合理的，完成“整套平账体系治理”不合理。**

**我建议你这样排期**

1. **今天周二，先做 2 件事**
   1. 把本地跑起来，确认启动链路、导入链路、平账链路能跑到哪一步
   2. 向测试要一条最小复现样例：输入数据、操作步骤、实际结果、期望结果

2. **周三，做定位和口径确认**
   1. 先只盯一个问题，不要同时开三四条线
   2. 把文档里的规则和产品确认清楚，尤其是：
      - 平账是“单账户扣”还是“可跨账户组合扣”
      - 按单笔出账还是按运单汇总扣
      - 余额不足时是跳过、部分扣，还是记待结算
      - 月结后修改/删除历史记录怎么处理
   3. 把复现过程固定成一份笔记，后面直接当验收依据

3. **周四，修复 + 最小回归**
   1. 先修最核心的 bug
   2. 如果来得及，加一个最小测试或一个对账脚本
   3. 再拿测试数据跑一遍，确认结果和口径一致

4. **周五，收尾和验收**
   1. 让测试按同一份数据复测
   2. 输出修复说明，明确这次修了什么，没修什么
   3. 把仍然存在的风险单独列出来，不要混在“已修复”里

**你从什么开始**

先从这三件事开始，顺序不要乱：

1. **找一个具体 bug**
   不要一上来研究整个系统。先拿“能复现的一个案例”开刀。

2. **确认规则**
   这项目最危险的不是代码复杂，而是口径不一致。  
   你前面给的文档里，“可跨账户扣” 和 “只能单账户扣” 其实就已经打架了。

3. **做对账表**
   我建议你边看边写一个小表，字段就够了：
   - 会员
   - 账户
   - 入账
   - 出账
   - 月初结余
   - 平账结果
   - 期望值
   - 实际值
   - 差异原因

这张表会让你很快看清到底是“代码错了”还是“规则没定”。

**更实际的工作方式**

你这几天别追求“大修”，而是按这个节奏：

- 先找测试
- 再找产品确认口径
- 然后用一组样本把 bug 复现出来
- 最后只修这一条链路

这个项目最适合的打法不是“写得更聪明”，而是“把规则钉死，把证据补齐”。

**我对你这个周五目标的判断**

如果你把范围收敛到“一个主要 bug + 一轮验证”，那是可以冲的。  
如果对方默认你把整个平账/结算体系都理顺，那周五前大概率不现实。

你现在最好的排期不是按“代码模块”排，而是按“风险”排：
- 先确认口径
- 再复现问题
- 再改最小路径
- 再做回归

这会比直接开改稳很多，也更容易让别人觉得你是个能控局的人。

我可以下一条直接帮你整理一个**周二到周五的每日执行清单**，按“上午/下午/交付物”写得更落地。
```



spring.datasource.druid.url 192.168.110.33:3306

```
jdbc:mysql://192.168.110.33:3306/financial?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
```

spring.datasource.druid.username financial

spring.datasource.druid.password financial
